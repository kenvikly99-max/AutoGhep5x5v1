name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build Project
      run: |
        # 1. Tạo cấu trúc thư mục tối giản
        mkdir -p app/src/main/java/com/example/autoghep
        mkdir -p app/src/main/res/values

        # 2. Tạo build.gradle siêu đơn giản (chỉ cần thiết)
        cat << 'EOF' > build.gradle
        buildscript {
            repositories {
                google()
                mavenCentral()
            }
            dependencies {
                classpath 'com.android.tools.build:gradle:8.1.0'
                classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.0'
            }
        }
        allprojects {
            repositories {
                google()
                mavenCentral()
            }
        }
        EOF

        # 3. Tạo app/build.gradle
        cat << 'EOF' > app/build.gradle
        apply plugin: 'com.android.application'
        apply plugin: 'kotlin-android'

        android {
            compileSdk 33
            defaultConfig {
                applicationId "com.example.autoghep"
                minSdk 24
                targetSdk 33
                versionCode 1
                versionName "1.0"
            }
        }
        dependencies {
            implementation 'androidx.core:core-ktx:1.9.0'
            implementation 'androidx.appcompat:appcompat:1.6.1'
        }
        EOF

        # 4. Tạo file bắt buộc settings.gradle
        echo "include ':app'" > settings.gradle

        # 5. Tạo file nguồn chính (MainActivity)
        cat << 'EOF' > app/src/main/java/com/example/autoghep/MainActivity.kt
        package com.example.autoghep
        import androidx.appcompat.app.AppCompatActivity
        import android.os.Bundle
        class MainActivity : AppCompatActivity() {
            override fun onCreate(savedInstanceState: Bundle?) {
                super.onCreate(savedInstanceState)
            }
        }
        EOF

        # 6. Tạo Manifest
        cat << 'EOF' > app/src/main/AndroidManifest.xml
        <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.example.autoghep">
            <application android:label="AutoGhep" />
        </manifest>
        EOF

        # 7. Cấp quyền và Build
        chmod +x gradlew || echo "gradlew not found, generating wrapper..."
        ./gradlew wrapper --gradle-version 8.0
        ./gradlew assembleDebug

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: AutoGhep-Debug
        path: app/build/outputs/apk/debug/app-debug.apk
